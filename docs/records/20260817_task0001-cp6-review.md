TASK0001 CP6 第三者視点レビューの記録
=====================================

- 日付: 2026-08-17
- 対象: `main..feat/task0001-foundation`（52 ファイル・+12651/-42）
- 観点: 10 / 10 実施（除外なし。性能効率とセキュリティ 2 観点は前提と攻撃者モデルを
  差し替えて縮退）
- 役割: レビューイの自己点検。GitHub への投稿は伴わない
- 確度: 高重要度 6 件は実測で確定（下表）。中・低は静的観察からの推論を含む
- 観点別の詳細レポート 10 本は `.claude/tmp/review/20260817-task0001/`（追跡外・短命）

この記録は、後続の TASK0003〜TASK0006 が根拠として参照する一次情報である。


## 全体の所見

**「黙って落とさない」規律が、記法の側には行き渡り、作品定義（YAML）の側と
ドロップダウンの一部に及んでいなかった。** 高重要度 6 件はすべてこの 1 本の筋に乗る。
記法の誤りは行番号つきで止まるのに、定義の誤りは終了コード 0 で通り、壊れた .sb3 が出た。

複数の観点が独立に同じ箇所へ到達した。作品定義の入力検査は 5 観点（信頼性・設計原則・
使用性・攻撃者視点・機能適合性）、ドロップダウンの照合は 3 観点（セキュリティ・攻撃者
視点・機能適合性）が指した。

強みとして複数観点から挙がったもの（壊してはいけない箇所）。

1. fail-closed の設計（解けないものは止め、部分的な成果物を残さない）
2. 決定論の作り込み（zip の固定・名前からの ID 導出。一次情報に紐づく）
3. 較正の記録（公式検証器と往復検査が何を見ないかを実測して検査の表に残してある）


## 高重要度 6 件と実測

裁定を仰ぐ前に実測した。レビューアはテスト実行を禁じられており、指摘は推論として返る。
推論のまま disposition を仰ぐと、偽だった場合に誤った選択肢で裁定を求めることになる。

| 指摘 | 直す前（実測）| 直した後（実測）| 直し方 |
|---|---|---|---|
| 選択肢の照合が `Object.prototype` の名前を素通し | `[toString v]` が問題 0 件で通り欄が `[null, null]`。公式検証も通す | 「選択肢に無いラベル」で停止 | 持ち物だけを見る |
| 固定選択肢の表が無い入力に日本語ラベルが入る | 背景の `BACKDROP` が `次の背景` | `next backdrop` | 表へ 3 選択肢を追加 |
| 触れた判定にスプライト名を書けない | 正当な記法が停止 | 通る。`端` は内部値へ | 名前の口を開ける |
| ターゲット名の重複・予約名が無検査 | 同名スプライトで先の宣言が消え、公式検証も通す | 停止 | 名前を検査 |
| 定義の値の型が無検査 | `表示: no` が文字列。公式検証器が弾く .sb3 が出る | 停止 | 値の形を検査 |
| 定義の鍵の綴り誤りが黙って通る | `スクリプト名:` で中身の無いスプライト | 停止 | 未知の鍵を申告 |

出典の裏取り: 背景の内部値は `parse-sb3-blocks` 0.5.2 の
`allMenus.looks_switchbackdropto`、日本語ラベルは `scratch-blocks` 2.1.19 の
`msg/scratch_msgs.js` の `locales['ja']`（既存の対応表と同じ出典）。


## disposition（2026-08-17・ユーザー裁定）

- 高 6 件と中 2 件（`.gitattributes`・Node の版の宣言）を今対応 → 反映済み
- 残り（中 12 件・低 40 件超）は別チケット起票 → TASK0003〜TASK0006 を起こした
- 学びの昇格は却下

今対応の反映後、検査を 7 件足し、破壊による確認を 3 通り行って 3 通りとも落ちた。
自動テストは 149 件すべて成功。`.gitattributes` だけは自動で確かめられない
（clone し直さないと再現しない）ため、覆えない範囲として申告する。


## 起票した先

| 作業書 | 束ねた指摘 |
|---|---|
| TASK0003 | 定義の仕様の明文化・引数の種別の噛み合い・ID の一意性・台帳読み取りの例外 |
| TASK0004 | 出荷経路での公式検証・台帳の版の照合・読む側の上限と循環検知 |
| TASK0005 | 手書き表の再導出照合・開発依存からの import・CI・`tsc --checkJs` |
| TASK0006 | 知識層の導線・誤りの報告が示す候補と再実行のコマンド |


## 見なかった範囲

レビューアにテスト実行を禁じたため、静的に確かめられない項目が観点ごとに 6〜9 件、
合計 68 件残った。主なもの。

- 第 2 の環境での実行（CI 不在。検証はすべて Windows x64 の 1 台のみ）
- CRLF の記法ファイルを解析器がどう扱うか
- フォントが欠けた環境での PNG の見え方
- 上流（scratchblocks・scratch-blocks）の新版での落ち方
- `npm audit` とネイティブ依存の取得経路
- 生成 SVG をブラウザで開いたときの実挙動

攻撃者視点は実質を伴う攻撃面を 4 面挙げたが、遠隔から突けるものは無く、depth
specialist の second wave は起動しないと判断した（供給網 1 面のみ価値があるが、
実行環境を要するため静的レビューでは尽きている）。

移植性のレビューアが制約違反を自己申告した（`node -v` / `npm -v` を各 1 回実行）。
指摘の成立は静的読み取りで足りており、結論への影響は無い。


## 要裁定の判断点

なし（2026-08-17 検分。10 観点の指摘全件を見た。高重要度 6 件と中 2 件は同日の
ユーザー裁定で今対応が確定し反映済み、残り 52 件超は TASK0003〜TASK0006 へ起票済み。
「見なかった範囲」の 68 件は実行環境を要する未検証項目であり、各作業書の到達基準へ
入っている。作業書の裁定を要する未決の判断点は残っていない）


__END__
