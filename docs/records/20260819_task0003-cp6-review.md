TASK0003 CP6 第三者視点レビューの記録
=====================================

- 日付: 2026-08-19（レビューの実施は 2026-08-18）
- 対象: `main..feat/TASK0003-definition-spec`（14 ファイル・+1059/-91）
- 観点: 10 / 10 実施（比例性ゲート full。起動 10 / 応答 10）
- 除外: レディネス評価（対象は成果物であり遷移でない）・攻撃面 depth specialist 9 種
  （ネットワーク境界も利用者境界も無く、実在する攻撃面が 1〜2 に限られる）
- 役割: レビューイの自己点検。GitHub への投稿は伴わない
- 確度: 高 3 件はメイン側で再現して確定（下表）。中・低は静的観察からの推論を含む
- 観点別の詳細レポート 10 本は `.claude/tmp/review/20260818-TASK0003/`（追跡外・短命）

この記録は、後続の TASK0007 が根拠として参照する一次情報である。


## 観点別の件数

各観点が自分で付けた重要度をそのまま数える。同一根拠へ着いた指摘は下の統合で 1 件へ
束ねるため、束ねた後の件数はこの合計と一致しない。

| 観点 | 高 | 中 | 低 | 判定不能 |
|---|---|---|---|---|
| 機能適合性 | 0 | 4 | 7 | 4 |
| 信頼性 | 0 | 6 | 6 | 3 |
| 保守性 | 0 | 5 | 7 | 2 |
| 使用性 | 1 | 7 | 5 | 4 |
| 設計原則 | 0 | 8 | 13 | 3 |
| 互換性 | 1 | 4 | 6 | 4 |
| 移植性 | 0 | 4 | 3 | 4 |
| 性能効率性 | 0 | 1 | 4 | 4 |
| セキュリティ | 0 | 1 | 5 | 3 |
| 攻撃者視点 | 0 | 2 | 6 | 3 |


## 全体の所見

**止める判断は正しかったが、止めた先の一手を誤った方へ押していた。** 高 3 件のうち 2 件は
本作業が作った不具合で、どちらも「検査そのもの」でなく「検査が出す言葉」と「検査の外へ
置いた範囲」に出ている。H1 は手掛かりに従うと黙って別のブロックになる .sb3 が出る経路、
H3 は正当な入力を止める経路である。

**測定コーパスの外にある類を 2 度踏んだ。** CP2 で「追跡下の記法 66 引数で誤検出 0 件」を
測り、それを根拠に「止める」裁定を得た。その後 CP5 で `%d.*`（数にドロップダウンが付く
入力）、CP6 で `#` + 16 進の文字列が、いずれもコーパスに 1 つも現れない形として漏れて
いた。コーパスは入力空間の標本であり、標本に無い類は何度数えても 0 件に見える。

**同じ指摘へ多くの観点が独立に到達したものが 1 件ある。** `--check` の古びた申告が
出どころを取り違える件は 7 観点が指摘した。目印は出どころごとに分けたのに、メッセージ
だけ「台帳と一致しない」で揃っていた。


## 高（3 件・いずれもメイン側で再現）

| # | 指摘 | 検出観点 | 再現 |
|---|---|---|---|
| H1 | 手掛かり文が黙って誤る .sb3 へ誘導する。`([記録 v] の長さ)` を止め、示した直し方に従うと `operator_length` に文字列 "記録" が入った .sb3 が問題 0 件で出る | 互換性・信頼性 | 済 |
| H2 | 空・コメントだけの `project.yaml` が終了コード 0 で「開くが何もしない .sb3」を出す。`render` は同じ状況を塞いでいる | 使用性 | 済（ターゲット 1 件・ブロック 0 件）|
| H3 | `#` + 16 進 3/6 桁の文字列を値の欄へ書くと正当な記法が止まる（`[#ff0000] と言う`）。回避手段が無く、ヒント文も誤導する | 機能適合性 | 済（3 桁・6 桁とも）|


## 中（束ねた後・20 件）

| # | 指摘 | 検出観点 |
|---|---|---|
| M1 | `--check` の食い違い申告が出どころを取り違える | 7 観点 |
| M2 | 「突き合わせの外に置いた引数 13 / 128」が誤り。正しくは 14 / 128 | 設計原則・機能適合性 |
| M3 | 既定値が独立に固定されていない（検査が実装から値を借りる）| 信頼性 |
| M4 | 「知らない形は素通ししない」判断と `%b` 除外の根拠に検査が無い | 信頼性・設計原則 |
| M5 | `knowledge` の書き込みが全か無かでない | 攻撃者視点・信頼性・セキュリティ |
| M6 | 「読めない台帳」の受け口が JSON 構文までしか守らない | 信頼性 |
| M7 | `スクリプト` の値が作品ディレクトリの外を指せる | セキュリティ・攻撃者視点 |
| M8 | 既存の正当な書き方（実測 5〜9 通り）を止める移行の告知が無い | 互換性・攻撃者視点 |
| M9 | `--dir` の座標系が 1 段上がった破壊的変更の記述が無い | 互換性・移植性 |
| M10 | 同一ブロックから 2 件出ると 2 件目が `行を特定できず` になる | 使用性 |
| M11 | 定義側の申告に位置情報が無く、`スプライト[0]` と 0 始まりで指す | 使用性 |
| M12 | 噛み合いの申告が引数を英字の台帳識別子で名指す | 使用性 |
| M13 | `変数` の初期値だけ日本語の申告を持たない | 使用性・攻撃者視点・互換性 |
| M14 | 鍵の綴りが 12 か所に散る／引数種別の対応表が 3 モジュールに分裂／書かれ方の類が生文字列で 4 か所 | 保守性・設計原則 |
| M15 | CRLF の知識層で `--check` が誤診し、直すと改行混在のまま緑になる | 移植性 |
| M16 | `スクリプト: sub\main.sbk` が Windows で通り Linux で ENOENT | 移植性 |
| M17 | 増やした停止が手順書の「詰まったときに見るもの」表と自己検査項目に無い | 使用性 |
| M18 | 生成しかしない経路が DOM 一式の立ち上げ費用を払う | 性能効率性 |
| M19 | `renderInto` の目印の既定値が渡し忘れを黙って通す／同名別概念が 2 組 | 保守性 |
| M20 | `%d.*` の全面除外により、選択肢に無いラベルが素通りする（既存）| 機能適合性 |

低は 62 件（重複を含む）。観点別レポートにある。


## 強み（実測で確かめられたもの）

- 既存作品の .sb3 は `main` と sha256 一致。TZ を振っても一致
- `CATALOG_MARKS` を据え置いたため既存 10 ページに差分なし
- `loadCatalog` の nullable 化に `src/` の全呼び出し元が追随
- ID 衝突検出は狙って崩そうとした 3 通りすべてで発火。走査範囲に欠けなし
  （種別の前置が固定なので種別跨ぎの衝突は原理的に起きない）
- `Object.hasOwn` による原型鍵の遮断。プロトタイプ汚染は非該当
- 噛み合い検査を独立に測り直した結果、台帳から合成した正準記法 111 件で誤検出 1 件
  （既知の綴り衝突）、メニュー引数 45 個への注入で 42 件検出
- 噛み合い検査の追加は O(1)。台帳の線形探索なし


## 裁定（2026-08-18・ユーザー）

| 対象 | disposition |
|---|---|
| H1・H2・H3 | 今対応 |
| M1・M2・M3・M4・M5・M8・M9 | 今対応 |
| M6・M7・M10〜M20・低 62 件 | 別チケットを 1 本起票（TASK0007）|


## 環境の観測（指摘とは別筋）

fan-out 中、作業ツリーの HEAD が topic ブランチと `main` の間で 2 往復切り替わった
（`git reflog` に記録）。5 観点が独立に観測し、4 観点は `git archive` や `git show main:`
で作業ツリーを触らない形へ切り替えて測定を続けた。残りは「ブランチ上での裏取りをして
いない」を判定不能として返した。specialist は読み取り専用の前提で並列に走らせており、
1 つが `git checkout` を打つと全観点の測定対象が入れ替わる。

この観測を受けて、共通コンテキストへ「作業ツリーを変えない。他のリビジョンは
`git show` / `git archive` で読む」を明記する規範を
`~/.claude/references/review-verification-protocols.md` と quality-self-review スキルへ
入れた（2026-08-19）。


## 要裁定の判断点

なし（2026-08-19 検分）。10 観点の高・中は 2026-08-18 のユーザー裁定で全件に
disposition が付いた（高 3 件と中 9 件を今対応、残る中を TASK0007 へ）。その後
M17（増やした停止が手順書の表と自己検査項目に無い）だけは 2026-08-19 の裁定で
TASK0006 へ移り、同作業で反映済みである。

低 62 件は TASK0007 の「stakeholder 未裁定の残課題」が引き取っており、本記録では未裁定を
持たない。


__END__
